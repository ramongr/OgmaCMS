<%= link_to t('pages.name'), admin_pages_path, class: "button small" %>
<%= link_to t('common.edit'), edit_admin_page_path(@page), class: "button small" %>
<%= link_to @page.publish ? t('common.unpublish') : t('common.publish'),  admin_page_publish_path(@page.id), method: :put, remote: true, class: "button small" + (@page.publish ? " alert" : "") %>