<%= form_for([:admin,@slider]) do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this admin_slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :selected %><br>
    <%= f.check_box :selected %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<div id="joyride-content">
  <ol class="joyride-list" data-joyride>
    <li data-id="slider_title" data-text="Ok" data-options="tip_location: top; prev_button: false">
      <p>Enter the name you want for the slider.</p>
      <p>You will add images in the next step.</p>
    </li>  
  </ol>
</div>