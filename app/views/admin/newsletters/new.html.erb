<h1 class="text-center"><%= t('newsletters.new.admin.title') %></h1>

<div class="row">
  <div id="newsletter-form" class="small-11 columns">
    <% if Setting.selected_languages.length < 2 %>
      <%= render 'form' %>
    <% else %>
      <%= render 'multilanguage_form' %>
    <% end %>
  </div>
  <div class="small-1 columns">
    <button id="joyride-init" class="small radius">?</button>
  </div>
</div>
