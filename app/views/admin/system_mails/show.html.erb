<br>
<div class="row">
  <%= link_to t('system_mails.name'), admin_system_mails_path, class: "button small" %>
  <%= link_to t('common.edit'), edit_admin_system_mail_path(@system_mail), class: "button small" %>

  <div class="small-12">
    <% if Setting.selected_languages.length < 2 %>
      <%= render 'single_language_show' %>
    <% else %>
      <%= render 'multilanguage_show' %>
    <% end %>
  </div>
</div>
