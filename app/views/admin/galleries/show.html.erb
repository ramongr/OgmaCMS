<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @gallery.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @gallery.description %>
</p>

<p>
  <strong>Date: </strong>
  <%= @gallery.date %>
</p>


<ul id="sortable-show">
  <% @added_images.each do |image| %>
    <li class="ui-state-default" id="<%= image.id %>">
      <img alt="default" href="<%= download_attachment_url(image.attachment)%>" src="<%= download_attachment_url(image.attachment)%>">
    </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_admin_gallery_path(@gallery) %> |
<%= link_to 'Back', admin_galleries_path %>
