<%= Setting.confirmation_instructions.gsub(/[*]name[*]/,@email)
                                     .gsub(/[*]confirmation_link[*]/,(link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token)))
                                     .html_safe %>