<%= form_for(@visitor_comment) do |f| %>
  <% if @visitor_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visitor_comment.errors.count, "error") %> prohibited this visitor_comment from being saved:</h2>

      <ul>
      <% @visitor_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if user_signed_in? %>
    <div class="field">
      <%= f.label :subject %><br>
      <%= f.text_field :subject %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <%= f.hidden_field :email, value: current_user.email %>
    <div class="field">
      <%= f.label :content %><br>
      <%= f.text_area :content, rows: 6 %>
    </div>
  <% else %>
    <div class="field">
      <%= f.label :subject %><br>
      <%= f.text_field :subject %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label :content %><br>
      <%= f.text_area :content, rows: 6 %>
    </div>
  <% end %>
  <div class="actions">
      <%= f.submit %>
  </div>
<% end %>
